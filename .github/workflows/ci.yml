name: Build with Vcpkg

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup Vcpkg
      uses: lukka/run-vcpkg@v11
      with:
        vcpkgFeatures: 'binarycaching'
        packages: 'lua sol2'